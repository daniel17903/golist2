{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install",
  "buildCommand": "export DATABASE_URL=\"$NEON_DATABASE_URL\" && npm run db:migrate && npm run build",
  "outputDirectory": "dist"
}
